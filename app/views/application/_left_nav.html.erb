<%= render "account_menu" if Jumpstart.config.team_accounts? && user_signed_in? && Current.other_accounts.any? %>

<% if user_signed_in? %>
  <%= nav_link_to "Policies", policy_documents_path %>
  <% if Current.account_admin? %>
    <%= nav_link_to "Issues", issues_path %>
  <% end %>
<% end %>
